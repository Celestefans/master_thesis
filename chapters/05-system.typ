#import "@preview/modern-ecnu-thesis:0.3.0": documentclass, indent, no-indent, word-count-cjk, total-words, bilingual-figure


= 一体化引导式图像超分系统
// 参考杨正罡毕业论文
== 引言

在前两章中，本文分别提出了基于视觉感知动态路由的 VP-Net 和融合多模态语义提示的 MAG-Net，从理论层面和实验层面验证了一体化引导式图像超分方法在磁共振图像超分、全色锐化及深度图超分等任务上的有效性。然而，这些先进的深度学习模型主要以代码脚本的形式存在，依赖复杂的命令行操作和环境配置，这对于缺乏深度学习背景的垂直领域专家与从业者——如医学影像医生、遥感测绘工程师以及自动驾驶研发人员来说，存在着较高的使用门槛。

为了弥合先进算法与行业应用之间的鸿沟，赋能垂直领域的高质量图像获取，本章基于 B/S（Browser/Server）架构，设计并实现了一个面向多领域专家的#strong[一体化引导式图像超分演示系统]。该系统集成了前文提出的核心算法模型，利用 Python Flask 框架搭建后端服务，结合 Bootstrap 前端框架构建直观的交互界面。系统不仅并通过封装复杂的模型推理过程，为不同领域的从业者提供了定制化的图像增强服务：协助医生获取更清晰的病灶细节，帮助测绘工程师获得高分辨率的卫星融合影像，以及为自动驾驶系统提供精细的深度感知信息。

本章将详细阐述该系统的需求分析、总体架构设计、功能模块实现以及开发环境配置。通过该系统的构建，旨在探索将一体化大模型技术转化为各行业生产力的工程实践方案。

== 需求分析

在开发本系统之前，本文深入调研了医学影像、遥感测绘及自动驾驶等领域的实际工作流，针对不同用户群体的核心痛点进行了详细的需求分析。本节将从功能性需求和非功能性需求两个维度进行阐述。

=== 功能性需求

本系统的核心目标是为各垂直领域的专家提供一个零代码、即插即用的图像超分增强平台。根据不同行业用户的应用场景，具体功能需求如下：

#heading(level: 4, numbering: none)[面向多领域任务的场景化配置]

   系统需支持针对不同行业场景的定制化任务选择：
   1. #strong[医学影像增强]：面向医学影像医生，支持导入低分辨率 MRI 扫描图像，通过超分算法重建高频纹理细节，辅助早期病灶筛查与诊断。
   2. #strong[遥感影像融合]：面向遥感与测绘工程师，支持全色影像与多光谱影像的融合处理，生成高分辨率多光谱数据，服务于地物分类与环境监测。
   3. #strong[深度图增强]：面向 3D 视觉与自动驾驶工程师，支持对低分辨率深度传感器数据进行上采样，恢复精细的几何结构信息，提升 SLAM 建图与避障精度。
   此外，系统需屏蔽复杂的模型超参数，仅保留倍率因子（$2 times, 4 times, 8 times, 16 times$）等业务相关选项。

#heading(level: 4, numbering: none)[交互式可视化与对比分析]
   专家用户往往需要对处理结果进行严格的质量评估。系统应提供直观的图像对比工具，支持“原始输入-算法输出”的实时切换或并排展示。特别地，针对医学和遥感图像的大图浏览需求，需支持局部放大查看功能，以便医生观察微小病变或工程师确认地物边缘。同时，系统应提供 PSNR、SSIM 等客观指标作为参考，辅助用户判断图像质量。

#heading(level: 4, numbering: none)[工程化批量处理]
   在实际的自动驾驶路测数据处理或大区域遥感制图中，工程师通常需要处理海量数据。系统需支持以文件夹为单位的批量上传与自动化队列处理功能。用户只需指定输入目录，系统后台即可自动调度资源完成所有图像的增强处理，并按原目录结构导出，显著提升工程效率。

#heading(level: 4, numbering: none)[全流程历史追溯]
   考虑到医疗诊断的可追溯性与测绘成果的归档要求，系统需具备完整的历史记录功能。每一次图像处理任务的输入源、所选算法、参数配置、处理耗时及结果指标通过数据库持久化存储。用户可随时回溯过往的处理记录，复现实验结果或导出分析报告。

#heading(level: 4, numbering: none)[多用户隔离与数据安全]
   由于医学影像涉及患者隐私，遥感数据可能涉及地理信息安全，系统必须具备严格的用户认证与权限管理机制。采用隔离的会话管理，确保不同科室医生或不同项目组工程师的数据互不可见，保障行业数据的安全性。

=== 非功能性需求

除了满足业务功能外，考虑到目标用户（医生、工程师）通常在高负荷环境下工作，系统还需满足以下非功能性指标：

#heading(level: 4, numbering: none)[极简操作与低学习成本]
   界面设计应遵循“少即是多”的原则，尽量减少非业务相关的干扰元素。操作流程应符合行业专家的直觉，例如采用医学影像常用的黑底风格或工程软件常见的布局，确保用户无需阅读手册即可上手使用。

#heading(level: 4, numbering: none)[高响应与实时反馈]
   对于大分辨率的遥感影像或 3D 深度图，处理过程可能较长。系统需提供精确的进度反馈机制（如进度条、剩余时间预估），避免用户产生焦虑。前端交互响应时间应控制在毫秒级，确保缩放、拖拽等操作的流畅性。

#heading(level: 4, numbering: none)[兼容性与易部署]
   考虑到医院内网环境或工程现场设备的多样性，系统应具备良好的跨平台兼容性。后端采用通用的 Python 环境，可部署在 Linux 服务器或高性能工作站上；前端基于标准 Web 技术，支持主流浏览器访问，无需在医生的诊断工作站或工程师的笔记本上安装额外客户端。





== 系统设计与实现

=== 总体架构

本系统采用经典的 B/S架构，如@fig:system-arch 所示，系统整体自上而下划分为展示层、服务层、算法层和数据层四个核心层次。各层之间通过明确的接口进行通信，保证了系统的高内聚与低耦合：
#heading(level: 4, numbering: none)[展示层]
直接面向终端用户，通过浏览器进行访问。采用响应式设计以适配不同尺寸的设备屏幕，并基于 Bootstrap UI 框架构建直观、易用的交互界面，提供图像上传、参数配置、结果可视化等功能。
#heading(level: 4, numbering: none)[服务层]
作为系统的中枢神经，基于轻量级的 Python Flask 服务器实现。负责对外提供 RESTful API 接口以接收前端请求，并进行核心的业务逻辑处理与任务调度，向下驱动算法层进行推理计算。
#heading(level: 4, numbering: none)[算法层]
系统的核心计算引擎，包含了精心设计的神经网络结构。该层集成了基于 PyTorch 框架实现的 MAG-Net 等一体化 AI 模型，负责执行具体的图像重建与增强任务。
#heading(level: 4, numbering: none)[数据层]
负责系统数据的持久化存储与管理。采用 MySQL 数据库存储用户账户信息与历史任务元数据，同时利用文件系统和硬盘存储海量的输入输出图像文件。

#figure(
  caption: [一体化引导式图像超分系统总体分层架构图。],
)[
#image("../images/05-system/分层架构.png", width: 85%)
] <system-arch>



=== 功能模块设计与实现

根据需求分析与实际业务流程，本文将系统划分为三个核心功能模块：用户认证模块、交互式图像复原与可视化模块以及批量任务管理与历史追溯模块。本节将详细阐述各模块的设计思路与关键实现。

==== 用户认证与权限管理模块
为了保障系统的安全性与数据隔离，系统实现了基于会话（Session）的用户认证机制。该模块主要包括用户登录、注销及会话状态保持功能。
#heading(level: 4, numbering: none)[登录验证]
用户输入账号与密码后，前端通过 POST 请求将凭证发送至后端 `/login` 接口。后端验证通过后，生成唯一的 Session ID 并写入浏览器 Cookie，作为后续请求的身份标识。

#heading(level: 4, numbering: none)[权限拦截]
通过 Flask 的 `login_required` 装饰器对核心业务接口（如上传、处理）进行保护，未登录用户的请求将被拦截并重定向至登录页。

#figure(
  caption: [系统用户登录与认证界面。],
)[
#image("../images/05-system/登录认证.png", width: 85%)
] <login>



==== 交互式图像复原与可视化模块
该模块是系统的核心交互界面，如@fig:system-single 所示，集成了任务配置、模型选择、图像上传、推理调度及结果对比分析的全流程功能，旨在为用户提供所见即所得的即时反馈体验。

#heading(level: 4, numbering: none)[多级算法策略配置]
为了满足不同层次用户的需求，模块支持灵活的算法策略选择。用户既可以选择基于 MAG-Net 的“一体化通用模型”策略，体验语义提示驱动的多模态处理；也可以切换至“任务专用模型”策略，调用各领域的 SOTA 模型进行对比验证。前端通过 JavaScript 实现动态联动，根据所选策略自动隐藏或显示相应的模型选项及提示词输入框。

#heading(level: 4, numbering: none)[语义提示词自动生成]
当选择 MAG-Net 模型时，系统利用内置规则引擎，根据当前选择的任务类型和缩放倍率，自动生成符合模型输入的英文语义提示（例如 "Pansharpening for satellite imagery, scale factor 4x"），极大降低了垂直领域专家的使用门槛。

#heading(level: 4, numbering: none)[异步推理与实时反馈]
用户上传图像并点击处理后，前端通过 AJAX 异步调用后端核心推理接口。系统采用非阻塞式设计，在后端调用 Python 算法脚本（如 `MAGNet.py`）进行计算的同时，前端展示加载动画与进度条，确保界面的流畅性。

#heading(level: 4, numbering: none)[多视图对比交互]
推理完成后，模块提供专业的图像对比工具。支持并排模式以展示整体差异，以及“叠加模式Overlay或卷帘模式，允许用户通过拖动滑块在同一坐标系下实时比对 LR 输入与 SR 输出的细节差异，辅助医生或工程师进行精细化质量评估。
#figure(
  caption: [交互式图像复原与可视化界面。],
)[
#image("../images/05-system/单张图片演示.png", width: 95%)
] <system-single>


==== 批量任务管理与历史追溯模块
针对工程化应用场景，本模块提供了高效率的数据处理与归档能力。

#heading(level: 4, numbering: none)[批量作业队列]
支持用户上传整个文件夹，系统后端遍历目录结构，建立任务队列，逐一调用算法模型进行处理，并将结果按原目录结构保存，大幅提高了大规模数据集的验证效率。

#figure(
  caption: [批量图像处理界面。],
)[
#image("../images/05-system/批处理任务.png", width: 95%)
] <system-task>

#heading(level: 4, numbering: none)[历史记录审计]
系统自动记录每次任务的元数据（任务ID、类型、耗时、PSNR/SSIM 指标）。历史页面以列表形式展示过往实验，支持按时间排序与筛选，用户可随时点击查看某次实验的详细参数与结果图，满足了实验可复现性的要求。

#figure(
  caption: [历史操作记录界面。],
)[
#image("../images/05-system/历史记录.png", width: 95%)
] <system-history>

== 数据库设计

为了支持系统的用户管理、批量任务调度以及历史记录追溯功能，本系统设计了基于关系型数据库（MySQL）的数据存储方案。核心数据表包括用户表（`users`）、任务批次表（`task_batches`）以及系统日志表（`system_logs`）。

=== 用户表设计
用户表主要用于存储系统用户的基本信息及认证凭据。考虑到系统需面向不同角色的用户（如管理员、医生、工程师），表中包含了角色字段以支持权限控制。表结构如@tbl:users 所示。

#include "../tables/05/users.typ"

=== 任务批次表设计
任务批次表是以目录为单位记录批量处理任务及其聚合状态的核心表。每一次批量上传操作都会生成一个唯一的批次号（Batch ID），系统通过该表追踪任务的处理进度及最终的质量指标（如平均 PSNR）。表结构如@tbl:task_batches 所示。

#include "../tables/05/task_batches.typ"

=== 系统日志表设计
系统日志表用于审计和追踪系统的关键操作或异常情况，确保系统的安全性与可维护性。表结构如@tbl:system_logs 所示。

#include "../tables/05/system_logs.typ"






== 本章小结

本章主要介绍了一体化引导式图像超分演示系统的设计与实现。针对垂直领域专家在使用先进深度学习模型时面临的技术门槛与操作痛点，本章首先从功能性与非功能性两个维度进行了详细的需求分析，明确了系统需具备多领域场景化配置、交互式可视化对比、工程化批量处理及全流程历史追溯等核心能力。

在此基础上，本章提出并实现了一种基于 B/S 架构的系统解决方案。系统自上而下划分为展示层、服务层、算法层和数据层，实现了高内聚低耦合的分层设计。文中详细阐述了关键功能模块的实现细节：通过基于会话的用户认证机制保障了数据安全；设计了交互式图像复原模块，将 MAG-Net 模型的语义提示生成机制封装于后端，使用户能够通过简单的界面操作即可调用复杂的一体化算法；构建了批量任务管理模块，有效满足了工程场景下大规模数据的处理需求。此外，本章还给出了支撑系统运行的数据库设计方案。

该系统的成功构建，不仅验证了前文提出的一体化引导式图像超分算法（MAG-Net）在实际应用环境中的可行性与鲁棒性，也为医学影像、遥感测绘及自动驾驶等垂直领域的图像增强任务提供了一套零代码、高效率、可视化的工程实践参考，有效缩小了前沿 AI 技术与行业实际应用之间的鸿沟。